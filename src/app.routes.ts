import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const APP_ROUTES: Routes = [
  { path: '', component: AppComponent, title: 'Indian Legal Bot' },
  // Add other routes as needed in the future
];
